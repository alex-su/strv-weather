<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="android:actionOverflowButtonStyle">@style/AppTheme.ActionBar.Overflow</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_action_back</item>
    </style>

    <style name="AppTheme.ActionBar" parent="android:Widget.ActionBar">
        <item name="android:background">@color/bg_action_bar</item>
        <item name="background">@color/bg_action_bar</item>
        <item name="android:titleTextStyle">@style/AppTheme.ActionBar.Title</item>
        <item name="titleTextStyle">@style/AppTheme.ActionBar.Title</item>
        <item name="android:icon">@drawable/action_bar_icon</item>
        <item name="icon">@drawable/action_bar_icon</item>
        <item name="android:displayOptions">homeAsUp|showHome|showTitle</item>
        <item name="displayOptions">homeAsUp|showHome|showTitle</item>
    </style>

    <style name="AppTheme.ActionBar.Title" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme.ActionBar.Overflow" parent="android:Widget.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_action_overflow</item>
        <item name="android:scaleType">center</item>
    </style>

</resources>
